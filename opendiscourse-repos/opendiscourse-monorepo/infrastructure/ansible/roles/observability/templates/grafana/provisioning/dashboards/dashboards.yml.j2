apiVersion: 1

providers:
  # Local dashboards
  - name: 'Local Dashboards'
    orgId: 1
    folder: 'Local'
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards

  # OpenSearch Dashboards
  - name: 'OpenSearch'
    orgId: 1
    folder: 'OpenSearch'
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards/opensearch

  # Prometheus Dashboards
  - name: 'Prometheus'
    orgId: 1
    folder: 'Prometheus'
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards/prometheus

  # Node Exporter Dashboards
  - name: 'Node Exporter'
    orgId: 1
    folder: 'Node Exporter'
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards/node-exporter

  # cAdvisor Dashboards
  - name: 'cAdvisor'
    orgId: 1
    folder: 'cAdvisor'
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards/cadvisor

  # Qdrant Dashboards
  - name: 'Qdrant'
    orgId: 1
    folder: 'Qdrant'
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards/qdrant

  # PostgreSQL Dashboards
  - name: 'PostgreSQL'
    orgId: 1
    folder: 'PostgreSQL'
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards/postgresql

  # Logging Dashboards
  - name: 'Logging'
    orgId: 1
    folder: 'Logging'
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards/logging

  # Alerting Dashboards
  - name: 'Alerting'
    orgId: 1
    folder: 'Alerting'
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards/alerting

  # Vector Bridge Dashboards
  - name: 'Vector Bridge'
    orgId: 1
    folder: 'Vector Bridge'
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards/vector-bridge
