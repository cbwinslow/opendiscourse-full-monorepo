# OpenDiscourse

This repository contains experiments for a media intelligence platform. It includes a Node.js API server, Python data tools, and simple HTML interfaces for running RAG queries.

Run `./install.sh` to set up the local environment and start services.
