# Change Log

## 2025-06-21 Repository enhancements
- Marked `copy-doc-repo.sh` and `copy-ssh-key.sh` as deprecated.
- Added `install.sh` for one-click setup of dependencies and services.
- Introduced simple query workspace HTML/JS for saving RAG queries.
- Added browser tracking script and server endpoint for event logging.
- Implemented session/cookie handling and request logging in `src/index.js`.
- Created minimal auth and track API routes.
- Extended `package.json` with session and logging dependencies.
- Added basic analysis scripts under `scripts/` (report builder, embedding demo, vector stats).
*** End of File
